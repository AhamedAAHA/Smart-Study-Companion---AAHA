{
  "name": "smart-study-companion",
  "version": "1.0.0",
  "private": true,
  "description": "AI learning platform for Sri Lankan students — ElevenLabs voice, Tamil explanations, mock viva",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:clean": "node -e \"const fs=require('fs');const p='client/.next';if(fs.existsSync(p))fs.rmSync(p,{recursive:true,force:true});\" && npm run dev",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "seed": "npm run seed --prefix server",
    "sync-demo-video": "node -e \"const fs=require('fs');const src='Rec 0004.mp4';const dest='client/public/demo/app-demo.mp4';if(!fs.existsSync(src))throw new Error('Missing '+src);fs.mkdirSync('client/public/demo',{recursive:true});fs.copyFileSync(src,dest);console.log('Synced',src,'->',dest);\"",
    "build": "npm run build --prefix server && npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
